var admin = require("./firebase_service");
const auth = firebase.auth;

class FirebaseAuthService {
  static async createUser(userData) {
    var googleLogin = new firebase.auth.GoogleLogin();
  }
} 
